<FONT COLOR="{{ style.attributes.color }}" FACE="{{ style.attributes.font }}" SIZE="{{ style.attributes.fontSize }}">
    {% if attribute.isStatic %}<U>{% endif %}
        {{ attribute }}
    {% if attribute.isStatic %}</U>{% endif %}
</FONT>
<BR ALIGN="LEFT"/>
