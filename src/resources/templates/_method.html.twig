{% if method.isAbstract %}<I>{% endif %}
    <FONT COLOR="{{ style.methods.color }}" FACE="{{ style.methods.font }}" SIZE="{{ style.methods.fontSize }}">
        {% if method.isStatic %}<U>{% endif %}
            {{ method }}
        {% if method.isStatic %}</U>{% endif %}
    </FONT>
{% if method.isAbstract %}</I>{% endif %}
<BR ALIGN="LEFT"/>
