{% set tag %}
    <TABLE CELLSPACING="0" BORDER="0" ALIGN="LEFT">
        {% include 'partials/_enum-name.html.twig' with {'definition': definition, 'theme': theme} %}
        {% include style.properties with  {'definition': definition, 'theme' : theme} %}
        {% include style.methods with {'definition': definition, 'theme' : theme} %}
    </TABLE>
{% endset %}
{{ tag|replace({ '\n': '', '\r': '' })|whitespace|raw }}
