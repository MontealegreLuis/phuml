<TR>
    <TD BORDER="{{ style.tableBorder }}" ALIGN="CENTER" BGCOLOR="{{ style.title.background }}">
        <FONT COLOR="{{ style.title.color }}" FACE="{{ style.title.font }}" POINT-SIZE="{{ style.title.fontSize }}">
            {{ definition.name }}
        </FONT>
    </TD>
</TR>
