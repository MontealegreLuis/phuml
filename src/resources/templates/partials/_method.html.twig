{% if method.isAbstract %}<I>{% endif %}
    <FONT COLOR="{{ theme.methods.color }}" FACE="{{ theme.methods.font }}" POINT-SIZE="{{ theme.methods.fontSize }}">
        {% if method.isStatic %}<U>{% endif %}
            {{ method }}
        {% if method.isStatic %}</U>{% endif %}
    </FONT>
{% if method.isAbstract %}</I>{% endif %}
<BR ALIGN="LEFT"/>
