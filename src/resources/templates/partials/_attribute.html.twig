<FONT COLOR="{{ theme.attributes.color }}" FACE="{{ theme.attributes.font }}" POINT-SIZE="{{ theme.attributes.fontSize }}">
    {% if attribute.isStatic %}<U>{% endif %}
        {{ attribute }}
    {% if attribute.isStatic %}</U>{% endif %}
</FONT>
<BR ALIGN="LEFT"/>
