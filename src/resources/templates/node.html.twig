{% spaceless %}
"{{ element.node.identifier }}" [label=<{% include 'uml/' ~ element.node.dotTemplate ~ '.html.twig' with { 'definition': element.node, 'style': style } %}> shape=plaintext color="{{ theme.lineColor }}"]
{% endspaceless %}
